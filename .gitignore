# Octoperator per-project settings (machine/project-specific, may contain repo + project IDs)
.claude/*.local.md

# hyperclaude research / plan / review artifacts (local working notes)
.hyperclaude/

# Octoperator parallel worktrees (one transient worktree per issue during parallel implement)
.octoperator/worktrees/

# OS / editor noise
.DS_Store
